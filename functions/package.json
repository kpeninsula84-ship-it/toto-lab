{
  "name": "toto-lab-functions",
  "description": "TotoLab Cloud Functions - data collection (fixtures, results), Betman odds + deadlines, Telegram notifications. AI analysis runs on GitHub Actions via the headless Claude Code CLI.",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "type": "module",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
